{
  "name": "p2p-chat",
  "version": "2.0.0",
  "description": "End-to-end encrypted peer-to-peer WebRTC chat - no server needed after initial page load",
  "author": "anusrnm",
  "license": "MIT",
  "keywords": ["p2p", "chat", "webrtc", "encryption", "privacy"],
  "scripts": {
    "start": "npx serve -s public -l 3000",
    "dev": "npx serve -s public -l 3000 --spa",
    "deploy:vercel": "vercel",
    "deploy:netlify": "netlify deploy --prod --dir=public",
    "build": "echo 'Static site - no build needed'"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
